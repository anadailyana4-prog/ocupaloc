name: E2E Staging

on:
  workflow_dispatch:

jobs:
  e2e-staging:
    name: E2E Smoke (staging required)
    runs-on: ubuntu-latest
    if: ${{ vars.ENABLE_E2E == 'true' && vars.ENABLE_E2E_STAGING == 'true' }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.7.1

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Pre-warm staging
        run: curl -s --max-time 90 -o /dev/null -w "Pre-warm HTTP %{http_code} in %{time_total}s\n" "${{ vars.PLAYWRIGHT_BASE_URL }}/${{ vars.PLAYWRIGHT_BOOKING_SLUG }}" || true

      - name: Run E2E smoke against staging
        env:
          PLAYWRIGHT_REQUIRE_EXECUTION: "true"
          PLAYWRIGHT_BASE_URL: ${{ vars.PLAYWRIGHT_BASE_URL }}
          PLAYWRIGHT_BOOKING_SLUG: ${{ vars.PLAYWRIGHT_BOOKING_SLUG }}
          PLAYWRIGHT_ENABLE_DEMO_SMOKE: ${{ vars.PLAYWRIGHT_ENABLE_DEMO_SMOKE }}
        run: pnpm run test:e2e
