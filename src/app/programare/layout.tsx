import { noIndexMetadata } from "@/lib/seo";

export const metadata = noIndexMetadata;

export default function ProgramareUtilityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
