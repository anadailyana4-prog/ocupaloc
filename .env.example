NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=https://www.ocupaloc.ro
NEXT_PUBLIC_CONTACT_EMAIL=contact@ocupaloc.ro
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_CLARITY_PROJECT_ID=
RESEND_API_KEY=
RESEND_FROM=
REMINDERS_CRON_SECRET=
BOOKING_CONFIRMATION_SECRET=

RATE_LIMITS_CRON_SECRET=
ALERT_WEBHOOK_URL=
ALERT_WEBHOOK_BEARER_TOKEN=
ALERT_COOLDOWN_MS=300000

# Playwright smoke (optional)
PLAYWRIGHT_BASE_URL=
PLAYWRIGHT_BOOKING_SLUG=
PLAYWRIGHT_ENABLE_DEMO_SMOKE=false

# Sentry (optional — errors not sent if blank)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Stripe Billing (optional — billing not enforced if STRIPE_SECRET_KEY is blank)
BILLING_ENABLED=false
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Owner Admin Portal (for first-time setup)
# Generate a secure token and use it to initialize the first owner admin
# curl -X POST https://your-domain/api/owner/init \
#   -H "Authorization: Bearer YOUR_INIT_SECRET_TOKEN" \
#   -H "Content-Type: application/json" \
#   -d '{"email": "your@email.com"}'
OWNER_INIT_SECRET=your-secure-init-token-here-change-in-production
OUTREACH_CRON_SECRET=
OUTREACH_SIGNING_SECRET=
OUTREACH_SMTP_HOST=mail.spacemail.com
OUTREACH_SMTP_PORT=465
OUTREACH_SMTP_USER=contact@ocupaloc.ro
OUTREACH_SMTP_PASSWORD=
OUTREACH_SMTP_SECURE=true
OUTREACH_IMAP_HOST=
OUTREACH_IMAP_PORT=
OUTREACH_IMAP_USER=
OUTREACH_IMAP_PASSWORD=
OUTREACH_IMAP_TLS=true
OUTREACH_IMAP_SENT_MAILBOX=Sent
OUTREACH_SEND_LIMIT_PER_HOUR=10
OUTREACH_SEND_LIMIT_PER_DAY=50
OUTREACH_FOLLOW_UP_DELAY_DAYS=4
OUTREACH_BATCH_SIZE=10
OUTREACH_SENDER_NAME=Echipa OcupaLoc.ro

TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_OWNER_IDS=
TELEGRAM_ADMIN_IDS=
TELEGRAM_OPERATOR_IDS=

# Optional for free lead import route (uses OpenStreetMap Overpass by default)
# Example body for POST /api/jobs/import-apify-run:
# {"city":"Bucuresti","limit":300}
